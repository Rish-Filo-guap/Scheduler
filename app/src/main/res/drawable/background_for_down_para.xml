<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="0"

        android:centerColor="#0c1121"
        android:centerX="0.17"
        android:endColor="@color/paraBackGroundColor"
        android:startColor="@color/paraBackGroundColor" />

    <!-- shape property for the view based on the value particular view would be drawn -->
    <corners
        android:bottomLeftRadius="20dp"
        android:bottomRightRadius="20dp"
        android:topLeftRadius="20dp"
        android:topRightRadius="20dp" />
    <!--<stroke android:width="10dp" android:dashGap="10dp" android:dashWidth="10dp" android:color="@color/white" />-->


    <!--  stroke for defining a border for view -->


    <!-- size element is used to define a design here height & width are same means square with some radius -->
</shape>